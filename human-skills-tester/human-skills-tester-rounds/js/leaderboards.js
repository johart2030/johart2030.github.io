import {db,collection,query,orderBy,limit,getDocs} from './firebase.js';
const tests={reaction:{name:'Reaction Time',unit:'ms',lower:true},number:{name:'Number Memory',unit:'level'},aim:{name:'Aim Trainer',unit:'s',lower:true,format:v=>(v/1000).toFixed(2)},sequence:{name:'Sequence Memory',unit:'level'},visual:{name:'Visual Memory',unit:'level'},typing:{name:'Typing Test',unit:'WPM'},math:{name:'Math Sprint',unit:'correct'}};
let selected='reaction';
const $=s=>document.querySelector(s),escape=v=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
async function load(key){selected=key;document.querySelectorAll('[data-board]').forEach(b=>b.classList.toggle('active',b.dataset.board===key));const test=tests[key],body=$('#leaderboardBody');$('#boardTitle').textContent=test.name;body.innerHTML='<div class="leaderboard-loading">Loading rankings…</div>';try{const q=query(collection(db,'leaderboards',key,'entries'),orderBy('value',test.lower?'asc':'desc'),limit(100)),snap=await getDocs(q);if(snap.empty){body.innerHTML='<div class="empty-state"><h3>No ranked scores yet</h3><p>Sign in and complete this test to claim the first spot.</p></div>';return}body.innerHTML=snap.docs.map((d,i)=>{const x=d.data(),value=test.format?test.format(Number(x.value)):Number(x.value);return `<div class="leaderboard-row"><strong class="rank">${i+1}</strong><span class="leader-name">${escape(x.displayName||'Player')}</span><strong class="leader-score">${value} <small>${test.unit}</small></strong></div>`}).join('')}catch(e){body.innerHTML=`<div class="empty-state"><h3>Leaderboard unavailable</h3><p>${escape(e.message)}</p></div>`}}
document.querySelectorAll('[data-board]').forEach(b=>b.onclick=()=>load(b.dataset.board));load(selected);
