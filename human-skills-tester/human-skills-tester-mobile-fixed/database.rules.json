{
  "rules": {
    ".read": false,
    ".write": false,
    "presence": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['state','lastChanged']) && newData.child('state').isString() && newData.child('lastChanged').isNumber()"
      }
    },
    "openRooms": {
      ".read": "auth != null",
      "$room": { ".write": "auth != null" }
    },
    "rooms": {
      "$room": {
        ".read": "auth != null && (data.child('status').val() === 'waiting' || data.child('players').child(auth.uid).exists())",
        ".write": "auth != null && (!data.exists() || data.child('players').child(auth.uid).exists() || data.child('status').val() === 'waiting')",
        ".validate": "!newData.exists() || (newData.hasChildren(['id','type','status','host','createdAt','players']) && newData.child('id').val() === $room && newData.child('createdAt').isNumber() && newData.child('players').numChildren() <= 2 && (newData.child('type').val() === 'reaction' || newData.child('type').val() === 'math' || newData.child('type').val() === 'typing') && (newData.child('status').val() === 'waiting' || newData.child('status').val() === 'countdown' || newData.child('status').val() === 'playing'))"
      }
    }
  }
}