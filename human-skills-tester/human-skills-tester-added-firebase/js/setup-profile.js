import {auth,onAuthStateChanged,claimDisplayName,profileFor,signOut,friendlyError} from './firebase.js';
const m=document.getElementById('message');onAuthStateChanged(auth,async user=>{if(!user)return location.replace('login.html');const p=await profileFor(user.uid);if(p)document.getElementById('displayName').value=p.displayName||'';document.getElementById('setupForm').onsubmit=async e=>{e.preventDefault();try{m.textContent='Saving…';await claimDisplayName(user,document.getElementById('displayName').value);location.replace('profile.html')}catch(err){m.textContent=friendlyError(err);m.className='auth-message bad'}}});
